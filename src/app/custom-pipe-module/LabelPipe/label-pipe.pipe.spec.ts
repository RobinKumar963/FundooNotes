import { LabelPipePipe } from './label-pipe.pipe';

describe('LabelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
