import { NotesSearchPipe } from './notes-search.pipe';

describe('NotesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NotesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
